<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyBuddy"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login"/>
        <activity android:name=".SignUp"/>
        <activity android:name=".StudyHomePage" />
        <activity android:name=".Shop"/>
        <activity android:name=".Inventory"/>
        <activity android:name=".RandomStore"/>
        <activity android:name=".Trade"/>
        <activity android:name=".Task"/>
        <activity android:name=".TaskAdapter"/>
        <activity android:name=".Settings" />
        <activity android:name=".Menu"/>
        <activity android:name=".Profile"/>
        <activity android:name=".TasksDone"/>
<!--        If java code is not in manifest then the sidebar I made appears when I try to access the xml for the java code-->
<!--        <activity android:name=".StudyBuddyApp" />-->
<!--        <activity-->
<!--            android:name=".Sidebar"-->
<!--            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />-->

    </application>

</manifest>